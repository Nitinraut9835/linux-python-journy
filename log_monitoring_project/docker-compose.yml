version: "3"

services:

  app:
    build: ./app
    container_name: flask_app
    ports:
      - "5000:5000"

  nginx:
    image: nginx
    container_name: nginx_server
    ports:
      - "8080:80"
    volumes:
      - ./nginx/nginx.conf:/etc/nginx/nginx.conf
    depends_on:
      - app
